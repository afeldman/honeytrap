[workspace]
resolver = "2"
members = [
    "crates/honeytrap-core",
    "crates/honeytrap-ai",
    "crates/honeytrap-deception",
    "crates/honeytrap-protocol",
    "crates/honeytrap-cli",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
authors = ["Anton Feldmann <anton.feldmann@gmail.com>"]
license = "MIT"
repository = "https://github.com/afeldman/honeytrap"
homepage = "https://github.com/afeldman/honeytrap"
documentation = "https://docs.rs/honeytrap"
description = "AI-powered Zero Trust Network Access with intelligent deception"
keywords = ["security", "ztna", "honeypot", "machine-learning", "network"]
categories = ["network-programming", "authentication", "cryptography"]

[workspace.dependencies]
# Async Runtime
tokio = { version = "1.35", features = ["full"] }
tokio-util = "0.7"

# Networking
quinn = "0.10"
rustls = { version = "0.21", features = ["dangerous_configuration"] }
webpki-roots = "0.25"

# Crypto
blake3 = "1.5"
chacha20poly1305 = "0.10"
x25519-dalek = "2.0"
rand = "0.8"
rand_core = "0.6"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
bincode = "1.3"

# ML / AI
ndarray = "0.15"
smartcore = { version = "0.3", features = ["serde"] }
linfa = "0.7"
linfa-trees = "0.7"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
tracing-appender = "0.2"

# CLI
clap = { version = "4.4", features = ["derive", "cargo", "env"] }
colored = "2.1"

# Error Handling
anyhow = "1.0"
thiserror = "1.0"

# Time
chrono = { version = "0.4", features = ["serde"] }

# Database (optional for logging)
sqlx = { version = "0.7", features = ["runtime-tokio-rustls", "sqlite"], optional = true }

# Metrics
prometheus = "0.13"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = true
panic = "abort"

[profile.dev]
opt-level = 0
